[build]
  command = "echo 'Starting CIAO Deployment Build...'"
  publish = "."
  functions = "functions"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
